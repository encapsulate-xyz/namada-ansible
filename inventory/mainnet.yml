---
# maintains mainnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:
    namada:
      hosts:
        ts.validator.namada.mainnet.encapsulate.xyz:
          type: validator
